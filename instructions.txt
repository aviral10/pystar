Instructions: 
1) Install Python from https://www.python.org/downloads/
2) While installing make sure to check "Add to PATH" option
3) Restart your machine
4) Run the following command, making sure your terminal is inside the current folder.
pip install -r requirements.txt
5) After the requirements are installed, run the script by the following command
streamlit run main.py
